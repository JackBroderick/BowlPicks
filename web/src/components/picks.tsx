import React, { FunctionComponent, useState } from 'react';
import { iComponentProps } from '../functions/interfaces';
import { Styles } from '../functions/constants';
import { Container, Row, Col, Image } from 'react-bootstrap';

export const Picks: FunctionComponent<iComponentProps> = function ({ ...props }): React.ReactElement {

    return (
        <Container fluid>
        </Container>
    )
}